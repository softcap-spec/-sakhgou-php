<?php
// config.php — скопируйте в config.php и заполните реальными данными
// НЕ КОММИТИТЬ config.php с паролями!
session_set_cookie_params([
  'lifetime' => 0,
  'path'     => '/',
  'secure'   => true,        // только HTTPS
  'httponly' => true,        // недоступно для JS
  'samesite' => 'Lax',      // защита от CSRF
]);
session_start();

define('SITE_NAME', 'СахGO');
define('SITE_URL', 'https://сахгоу.рф');
define('DB_HOST', 'localhost');
define('DB_NAME', 'your_db_name');
define('DB_USER', 'your_db_user');
define('DB_PASS', 'your_db_password');
define('DB_CHARSET', 'utf8mb4');
define('ITEMS_PER_PAGE', 12);
define('MAX_UPLOAD_SIZE', 2 * 1024 * 1024); // 2MB
define('UPLOAD_DIR', __DIR__ . '/uploads');

// Яндекс.Метрика: номер счётчика (например '12345678'). Пусто '' = счётчик выключен.
define('YANDEX_METRIKA_ID', '');

// Реквизиты оператора (футер, страница «Контакты», Политика конфиденциальности).
// Оператор — самозанятый (НПД), поэтому ОГРН/КПП не применимы.
// Пустые поля ('') скрываются на сайте.
define('REQUISITES', [
  'name' => 'Шаров А.А., самозанятый',
  'ogrn' => '',
  'inn' => '650112988764',
  'kpp' => '',
  'legal_address' => '',
  'postal_address' => '',
  'work_hours' => '',
  'phone' => '',
  'email' => 'support@sakh.su',
]);

// SMTP (optional — if set, emails go via SMTP; otherwise fallback to mail())
// Example (Yandex 360 / Selectel / Mail.ru):
// define('SMTP_HOST', 'smtp.yandex.ru');
// define('SMTP_PORT', 465);
// define('SMTP_SECURE', 'ssl'); // ssl | tls | none
// define('SMTP_USER', 'noreply@yourdomain.ru');
// define('SMTP_PASS', 'your_app_password');
// define('SMTP_FROM', 'noreply@yourdomain.ru');
// define('SMTP_FROM_NAME', 'СахGO');

// Уведомления «Макс» (Max Bot API) — секреты только на сервере, в git не попадают
// define('MAX_BOT_TOKEN', 'your_max_bot_token');
// define('MAX_WEBHOOK_SECRET', 'random_hex_32');
// define('MAX_BOT_NAME', 'СахGO');

// Robokassa (MerchantLogin, Пароль №1, Пароль №2 из личного кабинета)
// define('RK_MERCHANT_LOGIN', 'your_login');
// define('RK_PASSWORD1', 'password1');
// define('RK_PASSWORD2', 'password2');
// define('RK_IS_TEST', 1);

// Фискализация (54-ФЗ): передача состава чека в параметре Receipt. Включать после настройки чеков в кабинете Robokassa (Робочки/Мой налог, ИНН самозанятого).
// define('RK_RECEIPT_ENABLED', true);

define('CATEGORIES', json_encode([
  ['slug' => 'property',    'name' => 'Жильё',       'icon' => '🏠'],
  ['slug' => 'tour',        'name' => 'Туры',        'icon' => '🏔️'],
  ['slug' => 'fishing',     'name' => 'Рыбалка',     'icon' => '🎣'],
  ['slug' => 'rental_gear', 'name' => 'Снаряжение',  'icon' => '🎿'],
  ['slug' => 'car_rental',  'name' => 'Прокат авто', 'icon' => '🚗'],
], JSON_UNESCAPED_UNICODE));

// Quick-access array
$categories = [];
foreach (json_decode(CATEGORIES, true) as $c) {
  $categories[$c['slug']] = $c;
}
$current_category = 'all';
