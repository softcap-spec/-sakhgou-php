<?php
/**
 * version.php — единственный источник версии сборки.
 * Хранится в git → деплоится вместе с кодом (в отличие от config.php).
 * Инкрементится при каждом деплое (как кэш-бастинг ?v=N в header.php).
 */
define('APP_VERSION', '1.7.8');
